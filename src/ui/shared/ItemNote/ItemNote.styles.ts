import styled from 'styled-components';

const ItemNoteWrapper = styled.div`
display: flex;
flex-direction: row;
align-items: center;
gap: 10px;
.icon {
  display: flex;
  align-items: center;
}
.text {
  margin: 0;
}
`;

export default ItemNoteWrapper;
